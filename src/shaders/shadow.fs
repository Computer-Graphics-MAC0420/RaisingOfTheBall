// Fragment shader para renderização do shadow map
precision mediump float;

// Este shader é simples - apenas grava a informação de profundidade
void main() {
  // No modo shadow mapping, não precisamos de uma cor de saída específica
  // O framebuffer vai armazenar automaticamente os valores de profundidade
}
